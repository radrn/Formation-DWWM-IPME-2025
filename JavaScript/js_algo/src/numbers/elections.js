/**
 * Cette fonction doit déterminer si oui (true) ou (non)
 * il y aura des élections présidentielles en France au cours de l'année
 * donnée en paramètre.
 * Exemple :
 *  year = 2009 ----> FALSE, pas d'élections
 *  year = 2012 ----> TRUE, il y a bien eu des éléctions en 2007.
 * On part du principe que l'année donnée est forcément supérieure à 2007.
 * A titre d'information, il y a eu des élections en 2007.
 *
 * @param {number} year
 * @return {boolean}
 */
export function hasElections(year) {
    return true;
}
