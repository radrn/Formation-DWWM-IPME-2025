"use strict";

let robotCoord = { x: 0, y: 3 };

/**
 * This function should allow to control a Robot by giving order
 * A : move forwards
 * R : turn right
 * L : turn left
 *
 * @param orders {string}
 * @returns {{x: number, y: number}}
 */
const move = (orders) => {
};

console.log(move('aararaalaaalaaa'));

